sudo: required

language: python
python:
 - "3.6"
 
services:
  - docker

script:  
 - docker build . --tag myimage
 - docker push earthdatastore/eds-byoa:myimage
 - docker run myimage

after_script:
- docker images

